---
description: Thesis A lock — ksearch is a Metal kernel COMPILER (tinygrad-shaped), not a fused-op Metal catalog
alwaysApply: true
---

# Thesis A (non-negotiable)

ksearch is **tinygrad-shaped**, not metal-llm-server-shaped.

## Pipeline (required)

```
primitives → scheduler invents CALL boundaries → Kernel IR AST
  → generic MSL renderer (from AST only) → BEAM tilings
```

**Forbidden:** hand `rmsnorm.metal` / named `KirBody::RmsNorm` MSL templates / oracle shader paste / Graph catalog Ops (`MatVecQ4K*`, `AttnGqa*`, …).

Study source: `reference/tinygrad` UOp `Ops`. No `Ops.MATMUL` / `RMSNORM` / `FLASH_ATTN` / `GELU`.

## What may exist

- Graph sugar that **expands** to primitives (+ `Call`/`FuseHint` for schedule)
- Scheduler → `KernelKind` → **lower to KirStmt/KirExpr AST**
- Renderer that only walks the AST (`Load` dtype expand: F16→float, **Q4K→float**, …)
- BEAM OptOps as tilings on that AST

## Quant

- **Allowed:** `Load(dtype=Q4K|Q6K)` expand in the generic renderer (dequant-at-load to float, like F16→float). Q4_K/Q6_K weight buffers may stay packed; activations stay F16.
- **Forbidden:** hand `q4k_load` helpers pasted as named matvec kernels / Graph catalog `MatVecQ4K*` / oracle shader paste.
- **Forbidden:** hand named `matvec_q4k.metal` / catalog `Op::MatVecQ4K*` / fused Q4 matvec MSL templates.
- Eager F16 dequant is **not** required for Q4_K/Q6_K weights (optional for non-Q4/Q6 types).

## Oracle

Scoreboard + structure hints only. Never hot-path shader source.

## Source of truth

- `docs/DESIGN.md`
- This rule wins over speed work. Ask before any exception.
